fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy[asyncio]==2.0.29
asyncpg==0.29.0
alembic==1.13.1
pydantic==2.7.1
pydantic-settings==2.2.1
email-validator==2.3.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4's bcrypt backend probe is incompatible with bcrypt>=4.1
# (raises "password cannot be longer than 72 bytes" on init).
bcrypt==4.0.1
python-multipart==0.0.9
redis==5.0.4
httpx==0.27.0
python-dotenv==1.0.1
greenlet==3.0.3
# Optional: enables PDF report export. Needs system libs (pango/cairo); without
# it, /reports/.../export?fmt=pdf returns 501 and HTML export still works.
# weasyprint==62.3
# Optional: error tracking. Enabled only when SENTRY_DSN is set.
# sentry-sdk==2.7.1
