# Test/dev dependencies (install on top of requirements.txt).
#
# The API tests (tests/test_api_anonymity.py) need a Postgres test database.
# Default URL: postgresql+asyncpg://bhis:bhis_dev@localhost:5433/bhis_test
# Override with the TEST_DATABASE_URL env var (see tests/conftest.py).
-r requirements.txt
pytest==8.2.0
pytest-asyncio==0.23.7
